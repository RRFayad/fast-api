## Bookstore Proj

- CRUD Operations & Http Requests
  - Swagger UI is what gives us the URLs?

#### Run Server

source .venv/bin/activate
python3 -m uvicorn books:app --reload

url: http://127.0.0.1:8000/...

\*If we run `url/docs` if shows a Swagger UI

### Proj
